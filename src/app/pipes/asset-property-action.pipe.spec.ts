import { AssetPropertyActionPipe } from './asset-property-action.pipe';

describe('AssetPropertyActionPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetPropertyActionPipe();
    expect(pipe).toBeTruthy();
  });
});
