import { ModelingThreatsPipe } from './modeling-threats.pipe';

describe('ModelingThreatsPipe', () => {
  it('create an instance', () => {
    const pipe = new ModelingThreatsPipe();
    expect(pipe).toBeTruthy();
  });
});
