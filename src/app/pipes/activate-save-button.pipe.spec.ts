import { ActivateSaveButtonPipe } from './activate-save-button.pipe';

describe('ActivateSaveButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivateSaveButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
