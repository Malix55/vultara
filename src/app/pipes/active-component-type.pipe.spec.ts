import { ActiveComponentTypePipe } from './active-component-type.pipe';

describe('ActiveComponentTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveComponentTypePipe();
    expect(pipe).toBeTruthy();
  });
});
