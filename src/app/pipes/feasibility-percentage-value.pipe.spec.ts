import { FeasibilityPercentageValuePipe } from './feasibility-percentage-value.pipe';

describe('FeasibilityPercentageValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FeasibilityPercentageValuePipe();
    expect(pipe).toBeTruthy();
  });
});
