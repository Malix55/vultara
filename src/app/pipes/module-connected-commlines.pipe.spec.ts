import { ModuleConnectedCommlinesPipe } from './module-connected-commlines.pipe';

describe('ModuleConnectedCommlinesPipe', () => {
  it('create an instance', () => {
    const pipe = new ModuleConnectedCommlinesPipe();
    expect(pipe).toBeTruthy();
  });
});
