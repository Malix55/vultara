import { ModelModulesPipe } from './model-modules.pipe';

describe('ModelModulesPipe', () => {
  it('create an instance', () => {
    const pipe = new ModelModulesPipe();
    expect(pipe).toBeTruthy();
  });
});
