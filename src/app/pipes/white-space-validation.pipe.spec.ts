import { WhiteSpaceValidationPipe } from './white-space-validation.pipe';

describe('WhiteSpaceValidationPipe', () => {
  it('create an instance', () => {
    const pipe = new WhiteSpaceValidationPipe();
    expect(pipe).toBeTruthy();
  });
});
