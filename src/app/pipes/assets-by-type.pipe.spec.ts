import { AssetsByTypePipe } from './assets-by-type.pipe';

describe('AssetsByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AssetsByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
