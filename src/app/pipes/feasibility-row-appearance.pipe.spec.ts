import { FeasibilityRowAppearancePipe } from './feasibility-row-appearance.pipe';

describe('FeasibilityRowAppearancePipe', () => {
  it('create an instance', () => {
    const pipe = new FeasibilityRowAppearancePipe();
    expect(pipe).toBeTruthy();
  });
});
