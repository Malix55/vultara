import { ModelCommlinesPipe } from './model-commlines.pipe';

describe('ModelCommlinesPipe', () => {
  it('create an instance', () => {
    const pipe = new ModelCommlinesPipe();
    expect(pipe).toBeTruthy();
  });
});
