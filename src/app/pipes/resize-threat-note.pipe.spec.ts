import { ResizeThreatNotePipe } from './resize-threat-note.pipe';

describe('ResizeThreatNotePipe', () => {
  it('create an instance', () => {
    const pipe = new ResizeThreatNotePipe();
    expect(pipe).toBeTruthy();
  });
});
