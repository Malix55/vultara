import { MediaPipePipe } from './media-pipe.pipe';

describe('MediaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MediaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
