import { ThreatFrameColumnsPipe } from './theat-frame-columns.pipe';

describe('ThreatFrameColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new ThreatFrameColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
