import { ComponentFeatureAssetsPipe } from './component-feature-assets.pipe';

describe('FeatureAssetsPipe', () => {
  it('create an instance', () => {
    const pipe = new ComponentFeatureAssetsPipe();
    expect(pipe).toBeTruthy();
  });
});
