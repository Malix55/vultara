import { DisableMergePipe } from './disable-merge.pipe';

describe('DisableMergePipe', () => {
  it('create an instance', () => {
    const pipe = new DisableMergePipe();
    expect(pipe).toBeTruthy();
  });
});
