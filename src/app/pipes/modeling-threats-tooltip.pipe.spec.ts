import { ModelingThreatsTooltipPipe } from './modeling-threats-tooltip.pipe';

describe('ModelingThreatsTooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new ModelingThreatsTooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
