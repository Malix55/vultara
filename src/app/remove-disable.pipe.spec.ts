import { RemoveDisablePipe } from './remove-disable.pipe';

describe('RemoveDisablePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveDisablePipe();
    expect(pipe).toBeTruthy();
  });
});
