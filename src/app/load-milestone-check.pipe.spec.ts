import { LoadMilestoneCheckPipe } from './load-milestone-check.pipe';

describe('LoadMilestoneCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new LoadMilestoneCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
