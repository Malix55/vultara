import { AnalysisColorPipe } from './analysis-color.pipe';

describe('AnalysisColorPipe', () => {
  it('create an instance', () => {
    const pipe = new AnalysisColorPipe();
    expect(pipe).toBeTruthy();
  });
});
