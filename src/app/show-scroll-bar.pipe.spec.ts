import { ShowScrollBarPipe } from './show-scroll-bar.pipe';

describe('ShowScrollBarPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowScrollBarPipe();
    expect(pipe).toBeTruthy();
  });
});
